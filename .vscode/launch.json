{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug test",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/test", 
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}